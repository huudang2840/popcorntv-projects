import publicClient from "../client/public.client.js";

const genreEndpoints = {
  list: ({ mediaType }) => `person/${persomId}`,
  medias: ({ person }) => `person/${persomId}/medias`,
};

export default genreEndpoints;
